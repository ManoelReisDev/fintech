import { useDataContext } from "../Contexts/DataContext";
import DataRange from "./DataRange";

const Header = () => {
  return (
    <div>
      <DataRange />
    </div>
  );
};

export default Header;
